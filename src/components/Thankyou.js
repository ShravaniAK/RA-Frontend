import { Container, MenuItem, Typography } from "@mui/material";

export default function ThankYouPage () {
    return (
      <Container maxWidth="lg">
        <Typography variant="h3">Thank You for completing the test!</Typography>
       
      </Container>
    );
  };

  